/**
 * Refund Routes Placeholder
 */
const express = require('express');
const router = express.Router();

router.get('/', (req, res) => {
  res.json({ message: 'Refund routes - Coming soon' });
});

module.exports = router;
